{% render 'kj-card-styles' %}
{%- assign current_variant = product.selected_or_first_available_variant -%}

{%- comment -%}
  Media is grouped per shade via media alt text (alt == variant title).
  If any media matches the current variant, the gallery filters to the
  selected shade's set (reference behavior); otherwise all media shows.
{%- endcomment -%}
{%- assign has_shade_media = false -%}
{%- for media in product.media -%}
  {%- if media.alt == current_variant.title -%}
    {%- assign has_shade_media = true -%}
    {%- break -%}
  {%- endif -%}
{%- endfor -%}

<div class="kj-pdp" id="kj-pdp-{{ section.id }}" data-has-shade-media="{{ has_shade_media }}">
  <div class="kj-pdp-container">

    <!-- Gallery group: thumbnail rail + main image as one unit -->
    <div class="kj-pdp-gallery-group">

    <div class="kj-pdp-thumbs" data-kj-thumbs>
      {%- for media in product.media -%}
        {%- if media.media_type == 'image' -%}
          {%- assign hidden = false -%}
          {%- if has_shade_media and media.alt != current_variant.title -%}{%- assign hidden = true -%}{%- endif -%}
          <button
            type="button"
            class="kj-pdp-thumb{% if hidden %} kj-media-hidden{% endif %}"
            data-kj-pdp-thumb
            data-alt="{{ media.alt | escape }}"
            data-full="{{ media | image_url: width: 1400 }}"
            aria-label="View image"
          >
            <img src="{{ media | image_url: width: 160, height: 160, crop: 'center' }}" alt="{{ media.alt | escape }}" width="80" height="80" loading="lazy">
          </button>
        {%- endif -%}
      {%- endfor -%}
    </div>

    <!-- Main image -->
    {%- assign vto_shade_count = 0 -%}
    {%- assign vto_pairs = '' -%}
    {%- for variant in product.variants -%}
      {%- capture vv_hex -%}{% render 'kj-shade-hex', shade: variant.title %}{%- endcapture -%}
      {%- if vv_hex != blank -%}
        {%- capture vv_pair -%}"{{ variant.title | downcase | replace: '"', '' }}":"{{ vv_hex }}"{%- endcapture -%}
        {%- if vto_pairs == '' -%}
          {%- assign vto_pairs = vv_pair -%}
        {%- else -%}
          {%- assign vto_pairs = vto_pairs | append: ',' | append: vv_pair -%}
        {%- endif -%}
        {%- assign vto_shade_count = vto_shade_count | plus: 1 -%}
      {%- endif -%}
    {%- endfor -%}

    <div class="kj-pdp-main-media">
      {%- assign main_media = product.featured_image -%}
      {%- if has_shade_media -%}
        {%- for media in product.media -%}
          {%- if media.alt == current_variant.title -%}{%- assign main_media = media -%}{%- break -%}{%- endif -%}
        {%- endfor -%}
      {%- endif -%}
      {%- if main_media != blank -%}
        <img src="{{ main_media | image_url: width: 1400 }}" alt="{{ product.title | escape }}" class="kj-pdp-img" data-kj-pdp-main width="1400" height="{{ 1400 | divided_by: main_media.aspect_ratio | round }}">
      {%- else -%}
        {{ 'product-1' | placeholder_svg_tag: 'kj-pdp-placeholder' }}
      {%- endif -%}

      {%- if vto_shade_count > 0 -%}
        <!-- Virtual try-on runs INSIDE the gallery view (reference behavior):
             consent card -> getting ready -> live camera with close / save / compare -->
        <div class="kj-pdpvto-stage" data-kj-vto-stage hidden>

          <div class="kj-pdpvto-consent" data-kj-vto-consent hidden>
            <div class="kj-pdpvto-card">
              <button type="button" class="kj-pdpvto-card-x" data-kj-vto-close aria-label="Close">&times;</button>
              <h4>privacy policy</h4>
              <p>by clicking &quot;i agree&quot;, i consent to my device camera being used for the <strong>virtual try-on</strong> experience. camera frames are processed in real time on this device to render shades and are never stored or uploaded.</p>
              <p>close the try-on at any time to turn the camera off.</p>
              <label class="kj-pdpvto-agree-row"><input type="checkbox" data-kj-vto-check> i agree</label>
              <button type="button" class="kj-pdpvto-agree" data-kj-vto-agree disabled>agree</button>
              <button type="button" class="kj-pdpvto-nothanks" data-kj-vto-close>no thanks</button>
            </div>
          </div>

          <div class="kj-pdpvto-ready" data-kj-vto-ready hidden>
            <span class="kj-pdpvto-spinner" aria-hidden="true"></span>
            <span data-kj-vto-ready-label>getting ready</span>
          </div>

          <div class="kj-pdpvto-live" data-kj-vto-live hidden>
            <video data-kj-vto-video playsinline muted></video>
            <canvas data-kj-vto-canvas></canvas>
            <div class="kj-pdpvto-divider" data-kj-vto-divider hidden>
              <span class="kj-pdpvto-handle" data-kj-vto-handle>
                <svg width="7" height="10" viewBox="0 0 7 10" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M6 1 2 5l4 4"/></svg>
                <svg width="7" height="10" viewBox="0 0 7 10" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M1 1l4 4-4 4"/></svg>
              </span>
            </div>
            <button type="button" class="kj-pdpvto-x" data-kj-vto-close aria-label="Close try-on">&times;</button>
            <div class="kj-pdpvto-tools">
              <button type="button" class="kj-pdpvto-tool" data-kj-vto-snap aria-label="Save photo">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>
              </button>
              <button type="button" class="kj-pdpvto-tool" data-kj-vto-compare aria-label="Compare before and after">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><rect x="3.5" y="4.5" width="17" height="15" rx="1.5"/><line x1="12" y1="4.5" x2="12" y2="19.5"/></svg>
              </button>
            </div>
            <div class="kj-pdpvto-flash" data-kj-vto-flash></div>
          </div>

        </div>
      {%- endif -%}
    </div>

    {%- if vto_shade_count > 0 -%}
      <div class="kj-pdpvto-openrow">
        <button type="button" class="kj-pdpvto-open" data-kj-vto-open>
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>
          try me on
        </button>
      </div>
      <script type="application/json" data-kj-vto-map>{ {{ vto_pairs }} }</script>
    {%- endif -%}

    </div>

    <!-- Info rail -->
    <div class="kj-pdp-info">

      <nav class="kj-pdp-breadcrumbs">
        <a href="{{ routes.root_url }}">home</a> / <span>{{ product.title | downcase }}</span>
      </nav>

      <div class="kj-pdp-title-row">
        <h1 class="kj-pdp-title">{{ product.title }}</h1>
        {%- assign pdp_badge = '' -%}
        {%- for tag in product.tags -%}
          {%- assign tag_down = tag | downcase -%}
          {%- if tag_down == 'best seller' or tag_down == 'bestseller' or tag_down == 'new' or tag_down == 'favorite' or tag_down == 'favourite' -%}
            {%- assign pdp_badge = tag_down | replace: 'bestseller', 'best seller' -%}
            {%- break -%}
          {%- endif -%}
        {%- endfor -%}
        {%- if pdp_badge != blank -%}
          <span class="kj-pdp-badge">{{ pdp_badge }}</span>
        {%- endif -%}
      </div>

      {%- if section.settings.subtitle != blank -%}
        <p class="kj-pdp-subtitle">{{ section.settings.subtitle }}</p>
      {%- elsif product.type != blank -%}
        <p class="kj-pdp-subtitle">{{ product.type | downcase }}</p>
      {%- endif -%}

      {%- assign rating = product.metafields.reviews.rating.value.rating -%}
      {%- assign rating_count = product.metafields.reviews.rating_count -%}
      <div class="kj-stars{% if rating == blank %} kj-stars--placeholder{% endif %}" aria-hidden="true">
        {%- assign filled = rating | default: 5 | round -%}
        {%- for i in (1..5) -%}
          <span class="kj-star{% if i <= filled %} kj-star--filled{% endif %}">&#9733;</span>
        {%- endfor -%}
        {%- if rating_count != blank -%}
          <span class="kj-star-count">({{ rating_count }}) reviews</span>
        {%- endif -%}
      </div>

      {%- form 'product', product, id: 'kj-product-form', novalidate: 'novalidate' -%}
        <input type="hidden" name="id" value="{{ current_variant.id }}" data-kj-pdp-id>

        {%- unless product.has_only_default_variant -%}
          <p class="kj-pdp-shade-label">
            {{ product.options.first | downcase }}: <span data-kj-pdp-shade>{{ current_variant.title | downcase }}</span>
          </p>

          <!-- Shade dropdown -->
          {%- comment -%} Chip shows the real shade color: center-crop of the shade's swatch-smear photo when one exists. {%- endcomment -%}
          {%- assign current_chip = current_variant.featured_image -%}
          {%- for media in product.media -%}
            {%- if media.media_type == 'image' and media.alt == current_variant.title -%}
              {%- assign msrc = media.preview_image.src | default: '' -%}
              {%- if msrc contains 'LipMacro' or msrc contains 'lipmacro' -%}
                {%- assign current_chip = media.preview_image -%}
                {%- break -%}
              {%- endif -%}
            {%- endif -%}
          {%- endfor -%}
          {%- capture cur_hex -%}{% render 'kj-shade-hex', shade: current_variant.title %}{%- endcapture -%}
          <div class="kj-pdp-dropdown">
            <span class="kj-pdp-dropdown-chip">
              {%- if cur_hex != blank -%}
                <span class="kj-pdp-swatch-blank" style="background: {{ cur_hex }}; width: 28px; height: 28px; display: block; border-radius: 50%;"></span>
              {%- elsif current_chip != blank -%}
                <img src="{{ current_chip | image_url: width: 56, height: 56, crop: 'center' }}" alt="" width="28" height="28" data-kj-pdp-chip>
              {%- endif -%}
            </span>
            <select data-kj-pdp-select aria-label="{{ product.options.first | escape }}">
              {%- for variant in product.variants -%}
                <option value="{{ variant.id }}"{% if variant == current_variant %} selected{% endif %}>{{ variant.title | downcase }}</option>
              {%- endfor -%}
            </select>
            <svg class="kj-pdp-dropdown-caret" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 9l6 6 6-6"/></svg>
          </div>

          <!-- Family tabs -->
          {%- assign family_lines = section.settings.shade_families | newline_to_br | split: '<br />' -%}
          {%- if section.settings.shade_families != blank -%}
            <div class="kj-pdp-families" data-kj-families>
              <button type="button" class="kj-pdp-family kj-family-active" data-kj-family-tab data-family="all">all</button>
              {%- for line in family_lines -%}
                {%- assign parts = line | split: ':' -%}
                {%- if parts.size == 2 -%}
                  <button type="button" class="kj-pdp-family" data-kj-family-tab data-family="{{ parts[0] | strip | downcase }}">{{ parts[0] | strip | downcase }}</button>
                {%- endif -%}
              {%- endfor -%}
            </div>
          {%- endif -%}

          <!-- Shade chips -->
          <div class="kj-pdp-swatches">
            {%- for variant in product.variants -%}
              {%- assign v_family = '' -%}
              {%- for line in family_lines -%}
                {%- assign parts = line | split: ':' -%}
                {%- if parts.size == 2 -%}
                  {%- assign members = parts[1] | split: ',' -%}
                  {%- for m in members -%}
                    {%- assign m_clean = m | strip | downcase -%}
                    {%- assign v_clean = variant.title | downcase -%}
                    {%- if m_clean == v_clean -%}{%- assign v_family = parts[0] | strip | downcase -%}{%- endif -%}
                  {%- endfor -%}
                {%- endif -%}
              {%- endfor -%}
              {%- assign v_chip = variant.featured_image -%}
              {%- for media in product.media -%}
                {%- if media.media_type == 'image' and media.alt == variant.title -%}
                  {%- assign msrc = media.preview_image.src | default: '' -%}
                  {%- if msrc contains 'LipMacro' or msrc contains 'lipmacro' -%}
                    {%- assign v_chip = media.preview_image -%}
                    {%- break -%}
                  {%- endif -%}
                {%- endif -%}
              {%- endfor -%}
              {%- capture v_hex -%}{% render 'kj-shade-hex', shade: variant.title %}{%- endcapture -%}
              <button
                type="button"
                class="kj-pdp-swatch{% if variant == current_variant %} kj-pdp-swatch--active{% endif %}"
                data-kj-pdp-swatch
                data-id="{{ variant.id }}"
                data-title="{{ variant.title | escape }}"
                data-price="{{ variant.price | money }}"
                data-available="{{ variant.available }}"
                data-family="{{ v_family }}"
                {% if variant.featured_image != blank %}data-image="{{ variant.featured_image | image_url: width: 1400 }}"{% endif %}
                {% if v_chip != blank %}data-chip="{{ v_chip | image_url: width: 56, height: 56, crop: 'center' }}"{% endif %}
                aria-label="{{ variant.title | escape }}"
                {% unless variant.available %}data-soldout="true"{% endunless %}
              >
                {%- if v_hex != blank -%}
                  <span class="kj-pdp-swatch-blank" style="background: {{ v_hex }};"></span>
                {%- elsif v_chip != blank -%}
                  <img src="{{ v_chip | image_url: width: 56, height: 56, crop: 'center' }}" alt="" width="28" height="28" loading="lazy">
                {%- else -%}
                  <span class="kj-pdp-swatch-blank"></span>
                {%- endif -%}
              </button>
            {%- endfor -%}
          </div>
        {%- endunless -%}

        <!-- Qty + add to cart -->
        <div class="kj-pdp-buy-row">
          <div class="kj-pdp-qty">
            <button type="button" data-kj-qty-minus aria-label="Decrease quantity">&minus;</button>
            <input type="number" name="quantity" value="1" min="1" data-kj-qty aria-label="Quantity">
            <button type="button" data-kj-qty-plus aria-label="Increase quantity">+</button>
          </div>
          <button type="submit" name="add" class="kj-pdp-add" data-kj-pdp-add {% unless current_variant.available %}disabled{% endunless %}>
            {%- if current_variant.available -%}
              add to cart - <span data-kj-pdp-price>{{ current_variant.price | money }}</span>
            {%- else -%}
              sold out
            {%- endif -%}
          </button>
        </div>
      {%- endform -%}

      <!-- Accordions -->
      <div class="kj-pdp-accordions">
        <details class="kj-pdp-acc" open>
          <summary>details</summary>
          <div class="kj-pdp-acc-body">{{ product.description }}</div>
        </details>
        {%- if product.metafields.custom.ingredients != blank -%}
          <details class="kj-pdp-acc">
            <summary>ingredients</summary>
            <div class="kj-pdp-acc-body">{{ product.metafields.custom.ingredients | metafield_tag }}</div>
          </details>
        {%- endif -%}
        {%- if product.metafields.custom.how_to_use != blank -%}
          <details class="kj-pdp-acc">
            <summary>how to use</summary>
            <div class="kj-pdp-acc-body">{{ product.metafields.custom.how_to_use | metafield_tag }}</div>
          </details>
        {%- endif -%}
        {%- if section.settings.shipping_text != blank -%}
          <details class="kj-pdp-acc">
            <summary>shipping &amp; returns</summary>
            <div class="kj-pdp-acc-body">{{ section.settings.shipping_text }}</div>
          </details>
        {%- endif -%}
      </div>

      <!-- Benefits icons -->
      {%- if section.settings.benefits != blank -%}
        {%- assign benefit_items = section.settings.benefits | split: ',' -%}
        {%- assign benefit_icons = section.settings.benefit_icons | split: ',' -%}
        <div class="kj-pdp-benefits">
          {%- for b in benefit_items -%}
            <div class="kj-pdp-benefit">
              <span class="kj-pdp-benefit-icon{% if section.settings.benefit_icons != blank %} kj-pdp-benefit-icon--img{% endif %}">
                {%- if benefit_icons.size > 0 and section.settings.benefit_icons != blank -%}
                  {%- assign icon_index = forloop.index0 | modulo: benefit_icons.size -%}
                  <img src="{{ benefit_icons[icon_index] | strip }}" alt="" width="52" height="52" loading="lazy">
                {%- else -%}
                  {%- assign glyph = forloop.index0 | modulo: 3 -%}
                  {%- if glyph == 0 -%}
                    <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 3l1.8 5.2L19 10l-5.2 1.8L12 17l-1.8-5.2L5 10l5.2-1.8z"/></svg>
                  {%- elsif glyph == 1 -%}
                    <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 3c4 4.5 6 7.5 6 10.5a6 6 0 1 1-12 0C6 10.5 8 7.5 12 3z"/></svg>
                  {%- else -%}
                    <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M5 14c4-1 6-3 7-7 3 1 7 4 7 8a7 7 0 0 1-14 0z"/><path d="M5 21c2-3 4-4 7-5"/></svg>
                  {%- endif -%}
                {%- endif -%}
              </span>
              <span class="kj-pdp-benefit-label">{{ b | strip | downcase }}</span>
            </div>
          {%- endfor -%}
        </div>
      {%- endif -%}

    </div>
  </div>
</div>

<script>
  (function () {
    if (window.__kjPdpDelegated) return;
    window.__kjPdpDelegated = true;

    function selectVariant(root, data) {
      var idInput = root.querySelector('[data-kj-pdp-id]');
      if (idInput) idInput.value = data.id;
      var price = root.querySelector('[data-kj-pdp-price]');
      if (price && data.price) price.textContent = data.price;
      var shade = root.querySelector('[data-kj-pdp-shade]');
      if (shade) shade.textContent = (data.title || '').toLowerCase();
      var chip = root.querySelector('[data-kj-pdp-chip]');
      if (chip && data.chip) chip.src = data.chip;
      var sel = root.querySelector('[data-kj-pdp-select]');
      if (sel) sel.value = data.id;
      root.querySelectorAll('[data-kj-pdp-swatch]').forEach(function (s) {
        s.classList.toggle('kj-pdp-swatch--active', s.dataset.id === data.id);
      });
      var add = root.querySelector('[data-kj-pdp-add]');
      if (add) {
        var ok = data.available !== 'false';
        add.disabled = !ok;
        if (!ok) add.textContent = 'sold out';
      }
      // Gallery: filter thumbs to this shade if any match, else show all
      var thumbs = root.querySelectorAll('[data-kj-pdp-thumb]');
      var matches = [];
      thumbs.forEach(function (t) { if (t.dataset.alt === data.title) matches.push(t); });
      thumbs.forEach(function (t) {
        t.classList.toggle('kj-media-hidden', matches.length > 0 && t.dataset.alt !== data.title);
        t.classList.remove('kj-thumb-active');
      });
      var main = root.querySelector('[data-kj-pdp-main]');
      var first = matches[0] || thumbs[0];
      if (main && first) { main.src = first.dataset.full; first.classList.add('kj-thumb-active'); }
      else if (main && data.image) { main.src = data.image; }
      var url = new URL(window.location.href);
      url.searchParams.set('variant', data.id);
      window.history.replaceState({}, '', url.toString());
      document.dispatchEvent(new CustomEvent('kj:shade', { detail: { title: data.title || '' } }));
    }

    document.addEventListener('click', function (e) {
      var root;
      var swatch = e.target.closest('[data-kj-pdp-swatch]');
      if (swatch) {
        root = swatch.closest('.kj-pdp');
        selectVariant(root, swatch.dataset);
        return;
      }
      var thumb = e.target.closest('[data-kj-pdp-thumb]');
      if (thumb) {
        root = thumb.closest('.kj-pdp');
        var main = root.querySelector('[data-kj-pdp-main]');
        if (main) main.src = thumb.dataset.full;
        root.querySelectorAll('[data-kj-pdp-thumb]').forEach(function (t) {
          t.classList.toggle('kj-thumb-active', t === thumb);
        });
        return;
      }
      var tab = e.target.closest('[data-kj-family-tab]');
      if (tab) {
        root = tab.closest('.kj-pdp');
        var fam = tab.dataset.family;
        root.querySelectorAll('[data-kj-family-tab]').forEach(function (t) {
          t.classList.toggle('kj-family-active', t === tab);
        });
        root.querySelectorAll('[data-kj-pdp-swatch]').forEach(function (s) {
          s.classList.toggle('kj-media-hidden', fam !== 'all' && s.dataset.family !== fam);
        });
        return;
      }
      var minus = e.target.closest('[data-kj-qty-minus]');
      var plus = e.target.closest('[data-kj-qty-plus]');
      if (minus || plus) {
        root = (minus || plus).closest('.kj-pdp');
        var qty = root.querySelector('[data-kj-qty]');
        var v = parseInt(qty.value, 10) || 1;
        qty.value = Math.max(1, v + (plus ? 1 : -1));
      }
    });

    document.addEventListener('change', function (e) {
      var sel = e.target.closest('[data-kj-pdp-select]');
      if (!sel) return;
      var root = sel.closest('.kj-pdp');
      var swatch = root.querySelector('[data-kj-pdp-swatch][data-id="' + sel.value + '"]');
      if (swatch) selectVariant(root, swatch.dataset);
    });
  })();

  (function () {
    /* PDP Virtual Try-On — runs INSIDE the gallery view (reference behavior):
       try me on -> privacy consent -> getting ready -> live camera with lip
       tint, close, snapshot save, and a draggable before/after compare split.
       Shade comes from the PDP's own chips/dropdown via the kj:shade event.
       MediaPipe is lazy-loaded on first agree only. */
    if (window.__kjPdpVto) return;
    window.__kjPdpVto = { camera: null, faceMesh: null, color: null, loading: false, agreed: false, compare: false, split: 0.5, drag: false, map: null };
    var S = window.__kjPdpVto;

    function els() {
      var stage = document.querySelector('[data-kj-vto-stage]');
      if (!stage) return null;
      return {
        stage: stage,
        consent: stage.querySelector('[data-kj-vto-consent]'),
        ready: stage.querySelector('[data-kj-vto-ready]'),
        readyLabel: stage.querySelector('[data-kj-vto-ready-label]'),
        live: stage.querySelector('[data-kj-vto-live]'),
        video: stage.querySelector('[data-kj-vto-video]'),
        canvas: stage.querySelector('[data-kj-vto-canvas]'),
        divider: stage.querySelector('[data-kj-vto-divider]'),
        flash: stage.querySelector('[data-kj-vto-flash]')
      };
    }

    function shadeMap() {
      if (S.map) return S.map;
      var tag = document.querySelector('[data-kj-vto-map]');
      try { S.map = JSON.parse(tag.textContent); } catch (err) { S.map = {}; }
      return S.map;
    }

    function currentShadeColor() {
      var map = shadeMap();
      var label = document.querySelector('[data-kj-pdp-shade]');
      var key = label ? label.textContent.trim().toLowerCase() : '';
      if (map[key]) return map[key];
      for (var k in map) return map[k];
      return '#C98A78';
    }

    function setState(name) {
      var e = els();
      if (!e) return;
      e.consent.hidden = name !== 'consent';
      e.ready.hidden = name !== 'ready';
      e.live.hidden = name !== 'live';
    }

    function loadScript(src) {
      return new Promise(function (resolve, reject) {
        var s = document.createElement('script');
        s.src = src;
        s.crossOrigin = 'anonymous';
        s.onload = resolve;
        s.onerror = reject;
        document.head.appendChild(s);
      });
    }

    function onResults(results) {
      var e = els();
      if (!e || e.stage.hidden) return;
      if (!e.ready.hidden) setState('live');
      var canvas = e.canvas, ctx = canvas.getContext('2d');
      if (canvas.width !== e.video.videoWidth && e.video.videoWidth) {
        canvas.width = e.video.videoWidth;
        canvas.height = e.video.videoHeight;
      }
      ctx.save();
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      ctx.drawImage(results.image, 0, 0, canvas.width, canvas.height);
      if (results.multiFaceLandmarks && results.multiFaceLandmarks.length > 0) {
        var lm = results.multiFaceLandmarks[0];
        var outer = [61, 146, 91, 181, 84, 17, 314, 405, 321, 375, 291, 409, 270, 269, 267, 0, 37, 39, 40, 185, 61];
        var inner = [78, 95, 88, 178, 87, 14, 317, 402, 318, 324, 308, 415, 310, 311, 312, 13, 82, 81, 80, 191, 78];
        if (S.compare) {
          /* canvas is CSS-mirrored: right of the on-screen divider = left of the bitmap */
          ctx.beginPath();
          ctx.rect(0, 0, canvas.width * (1 - S.split), canvas.height);
          ctx.clip();
        }
        ctx.globalCompositeOperation = 'multiply';
        ctx.fillStyle = (S.color || '#C98A78') + '80';
        ctx.beginPath();
        outer.forEach(function (idx, i) {
          var pt = lm[idx];
          if (i === 0) ctx.moveTo(pt.x * canvas.width, pt.y * canvas.height);
          else ctx.lineTo(pt.x * canvas.width, pt.y * canvas.height);
        });
        inner.forEach(function (idx, i) {
          var pt = lm[idx];
          if (i === 0) ctx.moveTo(pt.x * canvas.width, pt.y * canvas.height);
          else ctx.lineTo(pt.x * canvas.width, pt.y * canvas.height);
        });
        ctx.fill('evenodd');
        ctx.globalCompositeOperation = 'source-over';
      }
      ctx.restore();
    }

    function cameraFail() {
      var e = els();
      if (e && e.readyLabel) e.readyLabel.textContent = 'camera unavailable — please allow camera access';
    }

    function startAR() {
      var e = els();
      if (!e || S.loading) return;
      var boot = function () {
        if (!S.faceMesh) {
          S.faceMesh = new window.FaceMesh({ locateFile: function (f) { return 'https://cdn.jsdelivr.net/npm/@mediapipe/face_mesh/' + f; } });
          S.faceMesh.setOptions({ maxNumFaces: 1, refineLandmarks: true, minDetectionConfidence: 0.5, minTrackingConfidence: 0.5 });
          S.faceMesh.onResults(onResults);
          S.camera = new window.Camera(e.video, {
            onFrame: function () { return S.faceMesh.send({ image: e.video }); },
            width: 480,
            height: 640
          });
        }
        var p = S.camera.start();
        if (p && p.catch) p.catch(cameraFail);
      };
      if (window.FaceMesh && window.Camera) { boot(); return; }
      S.loading = true;
      Promise.all([
        loadScript('https://cdn.jsdelivr.net/npm/@mediapipe/camera_utils/camera_utils.js'),
        loadScript('https://cdn.jsdelivr.net/npm/@mediapipe/face_mesh/face_mesh.js')
      ]).then(function () { S.loading = false; boot(); })
        .catch(function () { S.loading = false; cameraFail(); });
    }

    function closeVto() {
      var e = els();
      if (!e) return;
      e.stage.hidden = true;
      setState('none');
      S.compare = false;
      if (e.divider) e.divider.hidden = true;
      var cmp = e.stage.querySelector('[data-kj-vto-compare]');
      if (cmp) cmp.classList.remove('active');
      if (S.camera) S.camera.stop();
    }

    document.addEventListener('click', function (ev) {
      var open = ev.target.closest('[data-kj-vto-open]');
      if (open) {
        var e = els();
        if (!e || !e.stage.hidden) return;
        S.color = currentShadeColor();
        if (e.readyLabel) e.readyLabel.textContent = 'getting ready';
        e.stage.hidden = false;
        if (S.agreed) { setState('ready'); startAR(); }
        else { setState('consent'); }
        return;
      }
      if (ev.target.closest('[data-kj-vto-close]')) { closeVto(); return; }
      var agree = ev.target.closest('[data-kj-vto-agree]');
      if (agree && !agree.disabled) {
        S.agreed = true;
        setState('ready');
        startAR();
        return;
      }
      var snap = ev.target.closest('[data-kj-vto-snap]');
      if (snap) {
        var e2 = els();
        if (!e2 || !e2.canvas.width) return;
        var a = document.createElement('a');
        a.download = 'st-london-try-on.png';
        a.href = e2.canvas.toDataURL('image/png');
        a.click();
        if (e2.flash) {
          e2.flash.classList.remove('on');
          void e2.flash.offsetWidth;
          e2.flash.classList.add('on');
        }
        return;
      }
      var cmp = ev.target.closest('[data-kj-vto-compare]');
      if (cmp) {
        var e3 = els();
        if (!e3) return;
        S.compare = !S.compare;
        S.split = 0.5;
        e3.divider.hidden = !S.compare;
        e3.divider.style.left = '50%';
        cmp.classList.toggle('active', S.compare);
      }
    });

    document.addEventListener('change', function (ev) {
      var check = ev.target.closest('[data-kj-vto-check]');
      if (!check) return;
      var agree = document.querySelector('[data-kj-vto-agree]');
      if (agree) agree.disabled = !check.checked;
    });

    /* live shade sync: PDP chip/dropdown selection retints the try-on */
    document.addEventListener('kj:shade', function (ev) {
      var map = shadeMap();
      var key = ((ev.detail && ev.detail.title) || '').toLowerCase().trim();
      if (map[key]) S.color = map[key];
    });

    /* compare divider drag */
    document.addEventListener('pointerdown', function (ev) {
      if (!ev.target.closest('[data-kj-vto-handle]')) return;
      ev.preventDefault();
      S.drag = true;
    });
    document.addEventListener('pointermove', function (ev) {
      if (!S.drag) return;
      var e = els();
      if (!e) return;
      var r = e.stage.getBoundingClientRect();
      S.split = Math.min(0.88, Math.max(0.12, (ev.clientX - r.left) / r.width));
      e.divider.style.left = (S.split * 100) + '%';
    });
    document.addEventListener('pointerup', function () { S.drag = false; });
  })();
</script>

<style>
  .kj-pdp {
    background: #f8f6f2;
  }

  .kj-pdp-container {
    max-width: 1500px;
    margin: 0 auto;
    padding: 30px 20px 70px;
    display: grid;
    /* 50/50: gallery group | info */
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    gap: 60px;
    align-items: start;
  }

  .kj-pdp-gallery-group {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    align-items: flex-start;
    position: sticky;
    top: 90px;
  }

  /* "try me on" — small pill centered under the gallery image (reference) */
  .kj-pdpvto-openrow {
    flex-basis: 100%;
    display: flex;
    justify-content: center;
    padding-left: 86px; /* thumb rail 72px + 14px gap: center under the image */
  }

  .kj-pdpvto-open {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: #ffffff;
    border: 1px solid #d7d2cc;
    color: #222;
    border-radius: 4px;
    padding: 9px 16px;
    font-size: 13px;
    text-transform: lowercase;
    letter-spacing: 0.3px;
    cursor: pointer;
    transition: border-color 0.2s ease;
  }

  .kj-pdpvto-open:hover { border-color: #222; }

  /* The try-on stage overlays the gallery view */
  .kj-pdpvto-stage {
    position: absolute;
    inset: 0;
    z-index: 6;
  }

  /* display:flex on the state layers must not defeat the hidden attribute */
  .kj-pdpvto-stage[hidden],
  .kj-pdpvto-stage [hidden] {
    display: none !important;
  }

  /* State 1: privacy consent card over the product image */
  .kj-pdpvto-consent {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
  }

  .kj-pdpvto-card {
    position: relative;
    width: min(400px, 92%);
    background: #ffffff;
    box-shadow: 0 10px 34px rgba(0, 0, 0, 0.2);
    padding: 24px 26px;
    text-align: left;
  }

  .kj-pdpvto-card h4 {
    font-family: var(--font-heading--family);
    font-size: 15px;
    font-weight: 700;
    text-transform: lowercase;
    margin: 0 0 12px;
  }

  .kj-pdpvto-card p {
    font-size: 11.5px;
    line-height: 1.55;
    color: #444;
    margin: 0 0 10px;
  }

  .kj-pdpvto-card-x {
    position: absolute;
    top: 12px;
    right: 12px;
    background: none;
    border: none;
    font-size: 20px;
    line-height: 1;
    color: #222;
    cursor: pointer;
    padding: 4px;
  }

  .kj-pdpvto-agree-row {
    display: flex;
    align-items: center;
    gap: 7px;
    font-size: 12px;
    color: #222;
    margin: 14px 0;
    cursor: pointer;
  }

  .kj-pdpvto-agree-row input { accent-color: #222; }

  .kj-pdpvto-agree {
    display: block;
    width: 100%;
    background: #ffffff;
    border: 1px solid #222;
    padding: 10px 16px;
    font-size: 13px;
    text-transform: lowercase;
    cursor: pointer;
    transition: all 0.15s ease;
  }

  .kj-pdpvto-agree:disabled { border-color: #c9c9c9; color: #a5a5a5; cursor: not-allowed; }
  .kj-pdpvto-agree:not(:disabled):hover { background: #222; color: #fff; }

  .kj-pdpvto-nothanks {
    display: block;
    margin: 12px auto 0;
    background: none;
    border: none;
    font-size: 12px;
    color: #222;
    text-decoration: underline;
    text-transform: lowercase;
    cursor: pointer;
  }

  /* State 2: GETTING READY */
  .kj-pdpvto-ready {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 16px;
    background: #FBF1EE;
    color: #D98A93;
    font-family: var(--font-heading--family);
    font-size: 22px;
    font-weight: 700;
    letter-spacing: 3px;
    text-transform: uppercase;
    text-align: center;
    padding: 20px;
  }

  .kj-pdpvto-spinner {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    border: 3px dotted #E4B4B9;
    animation: kj-vto-spin 1.2s linear infinite;
  }

  @keyframes kj-vto-spin { to { transform: rotate(360deg); } }

  /* State 3: live camera in the gallery frame */
  .kj-pdpvto-live {
    position: absolute;
    inset: 0;
    background: #000;
    overflow: hidden;
  }

  .kj-pdpvto-live video { display: none; }

  .kj-pdpvto-live canvas {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transform: scaleX(-1); /* selfie mirror */
  }

  .kj-pdpvto-x {
    position: absolute;
    top: 12px;
    right: 12px;
    z-index: 4;
    width: 34px;
    height: 34px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.88);
    border: none;
    color: #222;
    font-size: 19px;
    line-height: 1;
    cursor: pointer;
  }

  .kj-pdpvto-tools {
    position: absolute;
    right: 12px;
    bottom: 14px;
    z-index: 4;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .kj-pdpvto-tool {
    width: 38px;
    height: 38px;
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.92);
    border: none;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #222;
    cursor: pointer;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.18);
  }

  .kj-pdpvto-tool.active { outline: 2px solid #222; }

  .kj-pdpvto-divider {
    position: absolute;
    top: 0;
    bottom: 0;
    left: 50%;
    width: 2px;
    background: #fff;
    z-index: 3;
    box-shadow: 0 0 6px rgba(0, 0, 0, 0.25);
  }

  .kj-pdpvto-handle {
    position: absolute;
    bottom: 16px;
    left: 50%;
    transform: translateX(-50%);
    width: 62px;
    height: 32px;
    border-radius: 16px;
    background: #fff;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    color: #222;
    cursor: ew-resize;
    touch-action: none;
    user-select: none;
  }

  .kj-pdpvto-flash {
    position: absolute;
    inset: 0;
    z-index: 5;
    background: #fff;
    opacity: 0;
    pointer-events: none;
  }

  .kj-pdpvto-flash.on { animation: kj-vto-flash 0.45s ease-out; }

  @keyframes kj-vto-flash {
    0% { opacity: 0.9; }
    100% { opacity: 0; }
  }

  .kj-pdp-gallery-group .kj-pdp-main-media {
    flex: 1;
  }

  .kj-pdp-thumbs {
    display: flex;
    flex-direction: column;
    gap: 12px;
    max-height: 640px;
    overflow-y: auto;
    scrollbar-width: none;
  }

  .kj-pdp-thumbs::-webkit-scrollbar { display: none; }

  .kj-pdp-thumb {
    width: 72px;
    height: 72px;
    padding: 0;
    border: 1px solid #DDD2C8;
    border-radius: 4px;
    overflow: hidden;
    background: #fff;
    cursor: pointer;
    flex: 0 0 auto;
  }

  .kj-pdp-thumb img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .kj-thumb-active {
    border-color: #393939;
  }

  .kj-media-hidden {
    display: none !important;
  }

  .kj-pdp-main-media {
    position: relative;
    aspect-ratio: 1 / 1;
    background: #fff;
    border: 1px solid #DDD2C8;
    border-radius: 6px;
    overflow: hidden;
  }

  .kj-pdp-img,
  .kj-pdp-placeholder {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .kj-pdp-breadcrumbs {
    font-size: 12px;
    color: #777;
    text-transform: lowercase;
    margin-bottom: 14px;
  }

  .kj-pdp-breadcrumbs a {
    color: #777;
    text-decoration: none;
  }

  .kj-pdp-breadcrumbs a:hover { text-decoration: underline; }

  .kj-pdp-title-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
  }

  .kj-pdp-title {
    font-family: var(--font-heading--family);
    font-size: 28px;
    font-weight: 700;
    text-transform: lowercase;
    letter-spacing: 0.3px;
    margin: 0;
  }

  .kj-pdp-badge {
    background: #C6A46C; /* palette gold */
    color: #ffffff;
    font-size: 12px;
    text-transform: lowercase;
    border-radius: 4px;
    padding: 5px 12px;
    white-space: nowrap;
  }

  .kj-pdp-subtitle {
    font-size: 14px;
    color: #444;
    text-transform: lowercase;
    margin: 6px 0 0;
  }

  .kj-pdp .kj-stars {
    margin: 10px 0 18px;
  }

  .kj-pdp .kj-star { font-size: 15px; }
  .kj-pdp .kj-star-count { font-size: 13px; }

  .kj-pdp-shade-label {
    font-size: 13px;
    text-transform: lowercase;
    color: #333;
    margin: 0 0 8px;
  }

  .kj-pdp-dropdown {
    position: relative;
    display: flex;
    align-items: center;
    gap: 10px;
    width: 320px;
    max-width: 100%;
    border: 1px solid #cfcfcf;
    border-radius: 4px;
    background: #fff;
    padding: 8px 12px;
    margin-bottom: 16px;
  }

  .kj-pdp-dropdown-chip img {
    display: block;
    width: 28px;
    height: 28px;
    border-radius: 50%;
    object-fit: cover;
  }

  .kj-pdp-dropdown select {
    appearance: none;
    -webkit-appearance: none;
    border: none;
    background: transparent;
    font-size: 14px;
    text-transform: lowercase;
    flex: 1;
    cursor: pointer;
    outline: none;
  }

  .kj-pdp-dropdown-caret {
    pointer-events: none;
    color: #555;
  }

  .kj-pdp-families {
    display: flex;
    gap: 18px;
    margin-bottom: 12px;
  }

  .kj-pdp-family {
    background: none;
    border: none;
    padding: 0;
    font-size: 14px;
    color: #555;
    text-transform: lowercase;
    cursor: pointer;
  }

  .kj-family-active {
    color: #000;
    font-weight: 700;
  }

  .kj-pdp-swatches {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-bottom: 24px;
  }

  .kj-pdp-swatch {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    border: 1px solid #ddd;
    padding: 1px;
    background: #fff;
    cursor: pointer;
    overflow: hidden;
    transition: border-color 0.15s ease;
  }

  .kj-pdp-swatch img,
  .kj-pdp-swatch-blank {
    display: block;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    object-fit: cover;
    background: #eee;
    transform: scale(1.8); /* zoom into the lip center so the chip reads as pure shade color */
  }

  .kj-pdp-dropdown-chip img {
    transform: scale(1.8);
  }

  .kj-pdp-swatch,
  .kj-pdp-dropdown-chip {
    overflow: hidden;
    border-radius: 50%;
  }

  .kj-pdp-swatch--active {
    border: 2px solid #393939;
  }

  .kj-pdp-swatch[data-soldout] { opacity: 0.35; }

  .kj-pdp-buy-row {
    display: flex;
    gap: 12px;
    margin-bottom: 26px;
  }

  .kj-pdp-qty {
    display: flex;
    align-items: center;
    border: 1px solid #cfcfcf;
    border-radius: 4px;
    background: #fff;
  }

  .kj-pdp-qty button {
    background: none;
    border: none;
    width: 34px;
    height: 42px;
    font-size: 16px;
    cursor: pointer;
    color: #333;
  }

  .kj-pdp-qty input {
    width: 34px;
    border: none;
    text-align: center;
    font-size: 14px;
    background: transparent;
    outline: none;
    -moz-appearance: textfield;
  }

  .kj-pdp-qty input::-webkit-outer-spin-button,
  .kj-pdp-qty input::-webkit-inner-spin-button {
    -webkit-appearance: none;
  }

  .kj-pdp-add {
    font-family: var(--font-heading--family);
    flex: 1;
    background: #222222; /* palette charcoal */
    color: #fff;
    border: none;
    border-radius: 4px;
    padding: 12px 20px;
    font-size: 14px;
    font-weight: 500;
    text-transform: lowercase;
    letter-spacing: 0.4px;
    cursor: pointer;
    transition: background 0.2s ease;
  }

  .kj-pdp-add:hover:not(:disabled) { background: #000000; }
  .kj-pdp-add:disabled { background: #999; cursor: not-allowed; }

  .kj-pdp-accordions {
    border-top: 1px solid #DDD2C8;
  }

  .kj-pdp-acc {
    border-bottom: 1px solid #DDD2C8;
  }

  .kj-pdp-acc summary {
    font-family: var(--font-heading--family);
    font-size: 17px;
    font-weight: 600;
    color: #91715B;
    text-transform: lowercase;
    letter-spacing: 0.3px;
    padding: 15px 0;
    cursor: pointer;
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .kj-pdp-acc summary::-webkit-details-marker { display: none; }
  .kj-pdp-acc summary::after { content: '+'; font-size: 18px; font-weight: 400; color: #91715B; }
  .kj-pdp-acc[open] summary::after { content: '\2212'; }

  .kj-pdp-acc-body {
    font-size: 14px;
    line-height: 1.7;
    color: #444;
    padding: 0 0 18px;
  }

  .kj-pdp-benefits {
    display: flex;
    flex-wrap: wrap;
    gap: 22px 26px;
    margin-top: 30px;
  }

  .kj-pdp-benefit {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    width: 68px;
  }

  .kj-pdp-benefit-icon {
    width: 52px;
    height: 52px;
    border: 1px solid #B69378;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #444;
    overflow: hidden;
    background: #fff;
  }

  .kj-pdp-benefit-icon img {
    width: 26px;
    height: 26px;
    object-fit: contain;
    display: block;
  }

  /* Icon PNGs that include their own circle: no double ring */
  .kj-pdp-benefit-icon--img {
    border: none;
    background: transparent;
  }

  .kj-pdp-benefit-icon--img img {
    width: 52px;
    height: 52px;
  }

  .kj-pdp-benefit-label {
    font-size: 11px;
    color: #444;
    text-align: center;
    text-transform: lowercase;
    line-height: 1.3;
  }

  @media screen and (max-width: 989px) {
    .kj-pdp-container {
      grid-template-columns: 1fr;
      gap: 20px;
      padding-top: 12px;
    }
    .kj-pdp-gallery-group {
      flex-direction: column;
    }
    .kj-pdp-thumbs {
      order: 2;
      flex-direction: row;
      max-height: none;
      overflow-x: auto;
      width: 100%;
    }
    .kj-pdp-main-media {
      order: 1;
      width: 100%;
    }
    .kj-pdpvto-openrow {
      order: 3;
      padding-left: 0;
    }
  }
</style>

{% schema %}
{
  "name": "KJ Main Product",
  "tag": "section",
  "settings": [
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#ffffff"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle (e.g. 0.12 oz | soft-matte finish)"
    },
    {
      "type": "textarea",
      "id": "shade_families",
      "label": "Shade families (one per line: label: shade, shade)"
    },
    {
      "type": "text",
      "id": "benefits",
      "label": "Benefit icons (comma separated)",
      "default": "clean, cruelty free, high pigmented, non-drying, paraben free, smudge-resistant, vegan"
    },
    {
      "type": "textarea",
      "id": "benefit_icons",
      "label": "Benefit icon URLs (comma separated; blank = built-in)"
    },
    {
      "type": "richtext",
      "id": "shipping_text",
      "label": "Shipping & Returns Text",
      "default": "<p>free uk shipping on orders over £40. easy returns within 30 days.</p>"
    }
  ]
}
{% endschema %}
