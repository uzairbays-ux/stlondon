<div class="kj-shop-category" style="background-color: {{ section.settings.bg_color }}; padding: {{ section.settings.padding_top }}px 0 {{ section.settings.padding_bottom }}px;">
  <div class="kj-category-container">
    {%- if section.settings.heading != blank -%}
      <h2 class="kj-category-heading" style="color: {{ section.settings.heading_color | default: section.settings.text_color }};">{{ section.settings.heading }}</h2>
    {%- endif -%}

    <div class="kj-category-list">
      {%- for block in section.blocks -%}
        <div class="kj-category-item" {{ block.shopify_attributes }}>
          <a href="{{ block.settings.collection.url | default: '#' }}" class="kj-category-link">
            <div class="kj-category-image-wrap">
              {%- if block.settings.image != blank -%}
                <img src="{{ block.settings.image | image_url: width: 700, height: 700, crop: 'center' }}" alt="{{ block.settings.title | escape }}" loading="lazy" class="kj-category-image" width="700" height="700">
              {%- elsif block.settings.collection.image != blank -%}
                <img src="{{ block.settings.collection.image | image_url: width: 700, height: 700, crop: 'center' }}" alt="{{ block.settings.title | escape }}" loading="lazy" class="kj-category-image" width="700" height="700">
              {%- else -%}
                <div class="kj-category-placeholder">{{ 'collection-1' | placeholder_svg_tag }}</div>
              {%- endif -%}
            </div>
            <span class="kj-category-title" style="color: {{ section.settings.text_color }};">
              {{ block.settings.title | default: block.settings.collection.title | default: 'Category' }}
            </span>
          </a>
        </div>
      {%- endfor -%}
    </div>
  </div>
</div>

<style>
  .kj-category-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 20px;
    text-align: center;
  }

  .kj-category-heading {
    font-family: var(--font-heading--family);
    font-size: 24px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 40px;
  }

  .kj-category-list {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }

  .kj-category-item {
    width: 100%;
  }

  .kj-category-link {
    text-decoration: none;
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
  }

  .kj-category-image-wrap {
    width: 100%;
    aspect-ratio: 1 / 1;
    border-radius: 8px;
    overflow: hidden;
    margin-bottom: 12px;
    background: #ffffff;
  }

  .kj-category-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.4s ease;
  }

  .kj-category-link:hover .kj-category-image {
    transform: scale(1.05);
  }

  .kj-category-placeholder svg {
    width: 100%;
    height: 100%;
    fill: #ccc;
  }

  .kj-category-title {
    font-family: var(--font-heading--family);
    font-size: 14px;
    font-weight: 700;
    text-transform: lowercase;
    letter-spacing: 0.3px;
    text-align: center;
    display: block;
  }

  @media screen and (max-width: 767px) {
    .kj-category-list {
      grid-template-columns: repeat(2, 1fr);
      gap: 12px;
    }
  }
</style>

{% schema %}
{
  "name": "KJ Shop by Category",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "SHOP BY CATEGORY"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading Color",
      "default": "#91715B"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Top Padding",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Bottom Padding",
      "default": 60
    }
  ],
  "blocks": [
    {
      "type": "category",
      "name": "Category",
      "settings": [
        {
          "type": "collection",
          "id": "collection",
          "label": "Collection"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Custom Image (Optional)"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Custom Title (Optional)"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "KJ Shop by Category",
      "blocks": [
        { "type": "category" },
        { "type": "category" },
        { "type": "category" },
        { "type": "category" }
      ]
    }
  ]
}
{% endschema %}
